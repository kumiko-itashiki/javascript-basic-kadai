//与えられた引数numを２乗にし、その値を戻り値をして返す関すを定義する：アロー関数
const square = (num) => {
  return num ** 2;
};

//与えられた引数numを２乗にし、その値を戻り値をして返す関すを定義する：関数宣言
// function square(num) {
//   return num ** 2;
// }

//関数の戻り値を出力する
console.log(square(30));
